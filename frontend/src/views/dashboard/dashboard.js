import { Grid } from '@mui/material';
import { gridSpacing } from '../../store/constants';


const Dashboard = () => <Grid container spacing={gridSpacing} >
    <h3>this is dashboard</h3>
</Grid>;

export default Dashboard;
